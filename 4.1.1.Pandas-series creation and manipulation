import pandas as pd

numbers = list(map(int,input().split()))
series = pd.Series(numbers)
grouped = series.groupby(series % 2).mean()

grouped.index = ['Even' if i==0 else 'Odd' for i in grouped.index]
print("Mean of even and odd numbers:")
print(grouped)
